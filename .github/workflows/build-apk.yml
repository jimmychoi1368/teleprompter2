name: 英语提词器APK构建（稳定版）

on: [push, workflow_dispatch]

jobs:
  build-apk:
    runs-on: ubuntu-22.04
    
    steps:
      # 1. 拉取代码
      - name: 拉取仓库代码
        uses: actions/checkout@v4

      # 2. 检查Vosk资源
      - name: 确认Vosk资源存在
        run: |
          echo "检查Vosk资源..."
          if [ ! -f "libs/vosk-android-0.3.70.aar" ]; then
            echo "错误：缺少Vosk AAR文件"
            exit 1
          fi
          if [ ! -d "assets/vosk-model-small-en-us-0.15" ]; then
            echo "错误：缺少Vosk模型文件"
            exit 1
          fi
          echo "✓ Vosk资源检查通过"

      # 3. 安装系统依赖
      - name: 安装系统依赖
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            python3-pip \
            git \
            zip \
            unzip \
            openjdk-11-jdk \
            autoconf \
            libtool \
            pkg-config \
            zlib1g-dev \
            libncurses5-dev \
            libncursesw5-dev \
            libtinfo5 \
            cmake \
            libffi-dev \
            libssl-dev \
            ninja-build
          echo "Java版本："
          java -version

      # 4. 安装Python依赖
      - name: 安装Python依赖
        run: |
          python3 --version
          pip3 install --user buildozer==1.5.0
          pip3 install --user cython==0.29.28
          echo "Buildozer版本："
          ~/.local/bin/buildozer --version

      # 5. 设置Android环境
      - name: 设置Android SDK/NDK
        run: |
          mkdir -p ~/android
          cd ~/android
          
          # 下载命令行工具
          wget -q https://dl.google.com/android/repository/commandlinetools-linux-9477386_latest.zip
          unzip -q commandlinetools-linux-9477386_latest.zip
          rm commandlinetools-linux-9477386_latest.zip
          
          # 设置环境变量
          echo "ANDROID_HOME=$HOME/android" >> $GITHUB_ENV
          echo "ANDROID_SDK_ROOT=$HOME/android" >> $GITHUB_ENV
          echo "PATH=$PATH:$HOME/android/cmdline-tools/bin" >> $GITHUB_ENV
          
          # 接受许可证
          yes | $HOME/android/cmdline-tools/bin/sdkmanager --licenses
          
          # 安装必要组件
          $HOME/android/cmdline-tools/bin/sdkmanager "platform-tools" "platforms;android-31" "build-tools;33.0.2" "ndk;23.1.7779620"
          
          echo "Android SDK安装完成"

      # 6. 构建APK
      - name: 构建APK
        env:
          ANDROID_HOME: ${{ env.ANDROID_HOME }}
          ANDROID_SDK_ROOT: ${{ env.ANDROID_SDK_ROOT }}
        run: |
          echo "开始构建APK..."
          echo "当前目录："
          pwd
          echo "文件列表："
          ls -la
          
          # 设置Buildozer配置
          export PATH=$PATH:~/.local/bin
          
          # 清理之前的构建
          ~/.local/bin/buildozer android clean
          
          # 开始构建（增加详细日志）
          ~/.local/bin/buildozer -v android debug 2>&1 | tee build.log
          
          # 检查是否生成APK
          if ls bin/*.apk 1> /dev/null 2>&1; then
            echo "✓ APK构建成功！"
            ls -lh bin/*.apk
          else
            echo "✗ APK构建失败"
            echo "构建日志最后100行："
            tail -100 build.log
            exit 1
          fi

      # 7. 上传APK
      - name: 上传APK文件
        if: success()
        uses: actions/upload-artifact@v4
        with:
          name: teleprompter-apk
          path: bin/*.apk
          retention-days: 7
